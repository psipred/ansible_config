1) Docker quickstart terminal

2) docker pull centos:7

3) docker run -it centos:7 bash

4) Run setup commands as per the ansible ogsinstall or
http://bioteam.net/2012/01/building-open-grid-scheduler-on-centos-rhel-6-2/

5) exit

6) commit the docker session
> docker commit 3a769844805a centos/ogs_compiled_installed

7) restart with
docker run -it centos/ogs_compiled_installed bash

8) restart with ogs ports mapped
docker run -it -p 6445:6445 -p 6444:6444 centos/ogs_installed bash

docker images
  list images
docker ps -l
  list last used container
docker ps -a
  list all containers

docker start c964b9681d8e
  start a container

docker attach c964b9681d8e
  get bash on a container you just started
  detach with ctrl-p ctrl-q

docker port <container> 8080
  test container port map

docker inspect <container>
  container info

delete docker images
  docker rmi image_name
delete docker containers
  docker rm containr_id

start aa container
------------------
docker run -it -p 6445:6445 -p 6444:6444 -p 8000:8000 -p80:80 centos/aa_installed bash

starting sge:
export PATH=$PATH:/opt/ogs_distribution/GE2011.11/bin/linux-x64/
export DRMAA_LIBRARY_PATH=/opt/ogs_distribution/GE2011.11/lib/
export C_FORCE_ROOT=TRUE
/root/rabbitmq_server-3.5.6/sbin/rabbitmq-server -detached
runuser -l postgres -c 'pg_ctl start -D /var/lib/pgsql/data/'
celery --app=analytics_automated_project.celery:app worker --loglevel=INFO -Q localhost,gridengine,celery --detach --pidfile=celery.pid
/etc/init.d/sgemaster.p6444 start
/etc/init.d/sgeexecd.p6444 start
qconf -as `hostname`
python3 manage.py runserver --settings=analytics_automated_project.settings.dev
